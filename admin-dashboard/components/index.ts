/**
 * Main Components Index
 * 
 * Central export point for all component categories.
 * Import from specific categories for better tree-shaking.
 */

// Interfaces - Components that interact and emit values
export * from './interfaces'

// Displays - Components that display values
export * from './displays'

// Widgets - Reusable components across modules
export * from './widgets'

// Layouts - Layout components
export * from './layouts'

// Modules - Bigger modules (import from specific module)
// Example: import { UserList } from '~/components/modules/users'

// Menus - Menu components (if shared)
// Example: import { MainMenu } from '~/components/menus'
